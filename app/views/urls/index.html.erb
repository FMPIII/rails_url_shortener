<h1> Pat's URLS </h1>

<% @urls.each do |url| %>
  <p>
    <%= link_to url.random_id, url_path(:id => url.random_id) %> => <%= url.original %>
  </p>

<% end %>

<%= link_to "Shorten Another URL", new_url_path, target: "_blank" %>
